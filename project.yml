name: ClipboardTranslatorIOS
options:
  bundleIdPrefix: com.personal
targets:
  ClipboardTranslatorIOS:
    type: application
    platform: iOS
    deploymentTarget: "16.0"
    sources: [ClipboardTranslatorIOS]
    info:
      path: ClipboardTranslatorIOS/Info.plist
      properties:
        UIBackgroundModes: [audio, fetch]
        UILaunchScreen: {}
